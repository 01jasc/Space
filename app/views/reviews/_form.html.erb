<%= simple_form_for([ facility, review ],
                    data: {
                      insert_in_list_target: "form",
                      action: "submit->insert-in-list#send"
                    }) do |f| %>
  <%= f.input :content, as: :text %>
  <%= f.input :rating, as: :integer %>
  <%= f.button :submit %>
<% end %>
